evaluation:
  primary_metric: recall

threshold:
  enabled: true
  objective: youden_j

quality_gates:
  recall:
    min: 0.90
  f1:
    min: 0.85
  roc_auc:
    min: 0.90
  latency_ms:
    max: 20.0
