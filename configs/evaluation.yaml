evaluation:
  primary_metric: pr_auc

threshold:
  enabled: true
  objective: youden_j

quality_gates:
  pr_auc:
    min: 0.90
  recall:
    min: 0.90
  f1:
    min: 0.85
  roc_auc:
    min: 0.90
  latency_ms:
    max: 20.0
